# Описание робота
1. Лидар имеет сектор 90гр и направлен вперед (в направлении робота).
  1.1 Ориентация робота по лидару:
  1.1. Перед робота по лидару = 180гр
  1.2. Сам лидар имеет угол обзора 90гр и иммет 360 точек. Разрешение 90/365 = 0,25гр.
  1.3. Сетор обзора в лево 180 + 45, а вправо 180 -45. Значит от 135 гр до 225 гр
  1.4. Сопоставим градусы с лидаром:
         tel->ranges[0], это соответствует 135гр  (будем считать с право от робота)
         tel->ranges[179], это соответствует 180гр  (будем считать с переди робота)
         tel->ranges[359], это соответствует 225гр  (будем считать с лева от робота)

2. чисел float32 (36 байт):
   - odom_x — одометрия X (м)
   - odom_y — одометрия Y (м)
   - odom_th — угол робота (рад)
   - vx — скорость по X (м/с)
   - vy — скорость по Y (м/с)
   - vth — угловая скорость (рад/с)
   - wx — угловая скорость по оси X (roll rate, рад/с) 🆕 
   - wy — угловая скорость по оси Y (pitch rate, рад/с)🆕 
   - wz — угловая скорость по оси Z (yaw rate, рад/с)🆕 
3. Кол-во точек лидара (N, uint32, 4 байта).
4. N значений float32 — массив дистанций лидара (м).

``` mermaid
flowchart LR
    subgraph ExternalEntities [Внешний мир]
        Lidar[Лидар]
        Odometry[Одометрия]
        Gyroscope[Гироскоп]
        SystemTime[Системное время]
    end

    subgraph System [Сущность Робота]
        A[Конечный автомат<br>Чёрный ящик]    
    end  

    subgraph Outputs [Состояния робота с параметрами]
        Forward[FORWARD<br>v=0.1-0.6, w=0.0]
        TurnRight[TURN_RIGHT<br>v=0.15, w=-1.0]
        TurnLeft[TURN_LEFT<br>v=0.15, w=1.0]
        Recover[RECOVER<br>v=-0.15, w=±1.0]
    end      

    Lidar -->|N точек расстояний| A
    Odometry -->|x, y, θ, vx, vy, vth| A
    Gyroscope -->|wx, wy, wz| A
    SystemTime -->|текущее время| A

    A --> Forward
    A --> TurnRight
    A --> TurnLeft
    A --> Recover
``` 

   
